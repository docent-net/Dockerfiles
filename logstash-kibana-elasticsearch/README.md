dockerfiles-fedora-logger (elasticsearch+kibana+logstash)
========================

Fedora dockerfile providing ElasticSearch + Kibana (over Nginx) + Logstash

Tested on Docker 0.8.0

This Dockerfile is still under development.
